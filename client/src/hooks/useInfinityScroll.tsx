import React, { useRef } from 'react'

const useInfinityScroll = () => {
    const observer = useRef<HTMLElement | null>(null);

    // const handleScroll = (([entry],ob) ) => {

    // }
  return (
    <div>useInfinityScroll</div>
  )
}

export default useInfinityScroll